<%= form_for(@biography) do |f| %>
  <% if @biography.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@biography.errors.count, "error") %> prohibited this biography from being saved:</h2>

      <ul>
      <% @biography.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="field">
    <%= f.label :slogan %><br>
    <%= f.text_area :slogan %>
  </div>
  <div class="field">
    <%= f.label :greeting %><br>
    <%= f.text_area :greeting %>
  </div>
  <div class="field">
    <%= f.label :website_name %><br>
    <%= f.text_area :website_name %>
  </div>
  <div class="field">
    <%= f.label :website_title %><br>
    <%= f.text_area :website_title %>
  </div>
  <div class="field">
    <%= f.label :bio_html %><br>
    <%= f.text_area :bio_html %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
